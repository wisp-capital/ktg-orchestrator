# Navigation Indexes

Generated cross-repo routing files for AI-max orchestrator agents.

- `manifest-index.md` is the human-readable route map.
- `manifest-index.json` is the machine-readable route map.

Refresh with the orchestrator's `nav-index` command.
Verify generated files with the orchestrator's `nav-check` command.
