# Status — ktg-orchestrator

**State is derived — do not hand-maintain this file.**

Run `python3 ~/repos/ai-max/scripts/amx.py --root . state` to generate the
live dashboard from `specs/` + git/PR + eval reports. Edit Specs
(`specs/*.spec.md`), not a dashboard.

(If you want a committed snapshot, write one with `amx state --write STATUS.md`;
it is generated output, not a source of truth.)
